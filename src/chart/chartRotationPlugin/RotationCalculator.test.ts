import { describe, it, expect } from 'vitest'

import { RotationCalculator } from './RotationCalculator'

describe('RotationCalculator.ts', () => {

  const tested = new RotationCalculator()

  describe('calculateTextRotationAngle', () => {

  }
}
